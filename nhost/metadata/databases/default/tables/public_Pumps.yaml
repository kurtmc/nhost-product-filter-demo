table:
  name: Pumps
  schema: public
select_permissions:
  - role: public
    permission:
      columns: [ID, ProductName, Power, FlowRate, Weight]
      filter: {}
  - role: user
    permission:
      columns: [ID, ProductName, Power, FlowRate, Weight]
      filter: {}
